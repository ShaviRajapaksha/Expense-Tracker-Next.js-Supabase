# Personal Expense Tracker 

A full-stack web application for managing personal finances, built with **Next.js 15 (App Router)** and **Supabase (PostgreSQL, Auth, & RLS)**.

This project focuses on leveraging Supabase directly from Next.js with Server Actions, direct database security via Row Level Security (RLS), and custom metrics dashboard views.

---

##  System Architecture

```text
                Next.js
                   │
        ┌──────────┴──────────┐
        │                     │
   React UI              Server Actions
        │                     │
        └──────────┬──────────┘
                   │
              Supabase
             ┌─────┴─────┐
             │           │
        PostgreSQL      Auth
             │
        expenses
             │
        categories
```

---

##  Tech Stack & Core Services

- **Framework:** Next.js (App Router, Server Components, Server Actions)
- **Language:** TypeScript
- **Styling:** Tailwind CSS
- **Backend & Database:** Supabase PostgreSQL
- **Authentication:** Supabase Auth (`/login`, `/register`)
- **Security:** Supabase Row Level Security (RLS)

---

##  Project Structure

```text
expense-tracker/
│
├── src/
│   ├── app/
│   │   ├── page.tsx
│   │   │
│   │   ├── login/
│   │   │   └── page.tsx
│   │   │
│   │   ├── register/
│   │   │   └── page.tsx
│   │   │
│   │   └── dashboard/
│   │       ├── page.tsx
│   │       └── expenses/
│   │           └── page.tsx
│   │
│   ├── components/
│   │   ├── Navbar.tsx
│   │   ├── ExpenseForm.tsx
│   │   ├── ExpenseList.tsx
│   │   └── DashboardCard.tsx
│   │
│   ├── lib/
│   │   └── supabase/
│   │       ├── client.ts
│   │       └── server.ts
│   │
│   └── types/
│       └── database.ts
│
├── public/
├── .env.local
├── package.json
└── next.config.ts
```

---

##  Database Schema & RLS Setup

### Database Tables

```text
auth.users
     │
     │ 1
     ▼
  profiles
     │
     │ 1
     │
     ▼
  expenses
     │
     │ many
     ▼
categories
```

#### SQL Definition (`expenses`):
```sql
CREATE TABLE expenses (
  id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  user_id UUID REFERENCES auth.users(id) ON DELETE CASCADE NOT NULL,
  title TEXT NOT NULL,
  amount NUMERIC(10, 2) NOT NULL,
  category TEXT NOT NULL,
  expense_date DATE NOT NULL,
  description TEXT,
  created_at TIMESTAMPTZ DEFAULT NOW()
);
```

### Row Level Security (RLS) Policies

To ensure database-enforced user isolation, RLS is applied directly on PostgreSQL:

```sql
-- Enable RLS
ALTER TABLE expenses ENABLE ROW LEVEL SECURITY;

-- Read Policy
CREATE POLICY "Users can view their own expenses" 
ON expenses FOR SELECT 
USING (auth.uid() = user_id);

-- Insert Policy
CREATE POLICY "Users can create their own expenses" 
ON expenses FOR INSERT 
WITH CHECK (auth.uid() = user_id);
```

---

## Learning Roadmap

- **Stage 1:** Next.js project setup & Supabase connection.
- **Stage 2:** Supabase Auth integration for signup/login routes.
- **Stage 3:** PostgreSQL schema creation & full CRUD operations.
- **Stage 4:** Row Level Security (RLS) implementation for isolated data access.
- **Stage 5:** Next.js Server Components and Server Actions integration.
- **Stage 6:** Dashboard construction (filtering, totals, and monthly expense breakdown).

---

## Getting Started

1. **Initialize Next.js App:**
   ```bash
   npx create-next-app@latest expense-tracker
   ```
   *Options selected:* TypeScript (Yes), ESLint (Yes), Tailwind CSS (Yes), `src/` (Yes), App Router (Yes), Turbopack (Yes), Import Alias (No).

2. **Install Supabase SDK:**
   ```bash
   cd expense-tracker
   npm install @supabase/supabase-js
   ```

3. **Configure Environment Variables:**
   Create `.env.local`:
   ```env
   NEXT_PUBLIC_SUPABASE_URL=your_supabase_project_url
   NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=your_publishable_key
   ```

4. **Run Development Server:**
   ```bash
   npm run dev
   ```
   Access application at `http://localhost:3000`.